# packages/shared

Shared TypeScript types consumed by `apps/dashboard`. From S1-17 onwards this
package will hold the generated TypeScript REST client produced from
`apps/api`'s OpenAPI document.

This package is currently an empty scaffold (S1-01).
