{
  "name": "@bullet/dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint configured in S1-16 Next.js scaffold)\""
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
